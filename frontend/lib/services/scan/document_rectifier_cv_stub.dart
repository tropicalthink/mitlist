import 'dart:typed_data';

/// Web stub — OpenCV/dart:ffi is unavailable on web; always returns null so
/// [DocumentRectifierService] uses its fail-soft path (returns original bytes).
Uint8List? rectifyDocumentCv(Uint8List jpegBytes) => null;
