import 'dart:typed_data';

/// Web stub — OpenCV/dart:ffi is unavailable on web; always returns null so
/// [CapturePreprocessorService] falls back to the legacy pure-Dart path.
Uint8List? enhanceCv(Uint8List jpegBytes) => null;
