# Implementation Plans — Intelligence Layer Audit

Generated by the improve skill on 2026-07-02 (commit `48e6867c`), from a focused
audit of the grocery intelligence layer (Flutter resolution stack, Go grocery
sync backend, `intelligence/` data pipeline). Execute in the order below unless
dependencies say otherwise. Each executor: read the plan fully before starting,
honor its STOP conditions, and update your row when done.

> Note: `frontend/plans/` is an older, separate plan set (offline/outbox
> hardening). This directory is independent of it.

## Execution order & status

| Plan | Title | Priority | Effort | Depends on | Status |
|------|-------|----------|--------|------------|--------|
| 001 | Fix backend grocery sync spine (column mismatch + error swallow + integration test) | P1 | M | — | DONE ✓v |
| 002 | Carry canonical item link through the main list composer | P1 | S | — | DONE ✓v |
| 003 | Stop correction memory from polluting itself | P1 | M | — | DONE ✓v |
| 004 | Wire the reject path so the resolver can unlearn | P2 | M | 003 | DONE ✓v |
| 005 | Reconcile client slug ids with server UUIDs | P1 | L | 001 | DONE ✓v |
| 006 | Close the calibration loop (eval one-command, holdout, CI, ship gate) | P2 | M | — | DONE ✓v |
| 007 | Batch the N+1 lookups on the scan hot path | P2 | S | — | DONE ✓v |
| 008 | Attach the grocery repository to SSE | P2 | S | 001 | DONE ✓v |
| 009 | Ensemble resolver + shared context in the shopping trip | P2 | S | — | DONE ✓v |
| 010 | Actionable alternatives (ids end-to-end, one-tap on tiles) | P2 | M | 003 | DONE ✓v |
| 011 | Scan-review store picker uses the shipped layout id space | P2 | M | — | DONE ✓v |
| 012 | Classifier↔seed parity gate in CI | P2 | M | — | DONE ✓v |
| 013 | Seed loader: sidecar version + off-isolate parse | P2 | S–M | — | DONE ✓v |
| 014 | Backend grocery hardening (tx, re-correction, pagination, validation) | P2 | M | 001 | DONE ✓v |
| 015 | Test the ensemble as shipped; log asset fail-soft | P2 | M | — | DONE ✓v |
| 016 | Pipeline docs match reality; drop dead TFLite-embedder branch | P3 | S | — | DONE ✓v |
| 017 | INVESTIGATE: aisle coverage (7%) — quantify, reduce loss, cost the fix | P3 | M | — | DONE ✓v |
| 018 | SPIKE: design household prior sync (purchase history + co-occurrence) | P3 | L | 001, 005, 014 | DONE ✓v |
| 019 | SPIKE: design server-delivered seed | P3 | L | 001, 005 | DONE ✓v |

Status values: TODO | IN PROGRESS | DONE | BLOCKED (with one-line reason) | REJECTED (with one-line rationale). `✓v` = done criteria re-verified by the advisor.

## Reconcile log — 2026-07-03 (HEAD `48e6867c`, work uncommitted in working tree)

All 19 plans verified DONE against the working tree:

- **Backend (001, 005, 014)**: `go build ./... && go vet ./...` clean; repositories, services, and handlers test packages all pass; `-run TestGrocery -v` shows 5 PASS, 0 SKIP (round-trip, unseeded-canonical correction, re-correction, pagination drain, validation).
- **Frontend (002–004, 007–011, 013, 015)**: `dart analyze lib/` clean; full `flutter test` = 434 pass / 1 skip / 1 fail. The single failure (`test/welcome_screen_test.dart` "guest continue with invite queues join landing navigation") is **pre-existing at HEAD and unrelated** — it is the known deliberately-deferred invited-guest case (pending a product decision; see the 2026-06 baseline-greening work). No plan or working-tree change touches the welcome screen.
- **Pipeline (006, 012, 016, 017)**: `intelligence/eval.sh` exits 0 end-to-end and prints fitted-vs-default holdout metrics; fitted weights lose to defaults on coverage@auto (0.368 vs 0.526), so the absent `frontend/assets/grocery/resolution_weights.json` is the correct NOT-SHIPPED outcome. `check_classifier_parity.py` → PASS, 0.00% orphans, CI step present. `seed.version.json` (v5) matches `seed.json`. No `grocery_embeddings.tflite` references remain. Plan 017 Findings section present with before/after resolution counts (823→871 of 978 rows).
- **Spikes (018, 019)**: both DESIGN docs exist with recommendations, measured payload sizes (019), and implementation slices.

Environment caveat: this host lacks the `libsqlite3.so` dev symlink (only `libsqlite3.so.0` present), so drift-backed `flutter test` and `eval.sh` fail out of the box. Workaround used for verification: symlink `libsqlite3.so → libsqlite3.so.0` in a scratch dir and run with `LD_LIBRARY_PATH` pointing at it (or install `libsqlite3-dev`).

Nothing BLOCKED, nothing stale, no drifted TODOs. The backlog is fully executed; next `/improve` run should start a fresh audit rather than reconcile.

## Dependency notes

- **001 first.** It unblocks 005, 008, 014, and makes every server-side claim testable (real-schema integration harness).
- **003 before 004 and 010** — both build on the `userConfirmed` flag and the unified normalisation it introduces.
- **005 after 001** — the id bridge is meaningless while every write 500s. 014's transaction wrapper must contain 005's canonical upsert if 014 lands second (noted in both plans).
- **002 is independent and the highest user-visible leverage** — it turns on purchase signals/restock/aisle sorting for typed items; do it early regardless of backend order.
- **018/019 are gated spikes** — do not start them before their dependencies are DONE; their designs assume the fixed cursor, the UUIDv5 contract, and pagination.
- 006, 007, 009, 011, 012, 013, 015, 016, 017 are mutually independent; interleave freely.
- Files shared across plans (expect merge coordination): `scan_review_screen.dart` (003, 004, 010, 011), `grocery_repository.dart` (004, 005, 014), `grocery_service.go`/`grocery_repo.go` (001, 005, 014), `intelligence/README.md` (006, 012, 016).

## Findings considered and rejected (do not re-audit)

- **Confidence thresholds unused** — false: `scan_pipeline_service.dart:146-149` feeds `autoThreshold`/`reviewThreshold` into `ConfidenceService`, driving the review tile states.
- **`eval_harness.dart` / `confidence_service` / `suggestion_service` / `grocery_suggestion_service` dead code** — all wired; verified call sites.
- **Static-embedding worker isolate hazards** — reviewed: dedicated reply ports, 2 s timeout fallback, always-reply catch, RootIsolateToken-gated spawn. Sound. (Minor CPU waste in `cosine`/hand-rolled sqrt — not worth a plan.)
- **IDOR on grocery endpoints** — none: all three handlers enforce group membership via `requireMembership` before data access.
- **Legacy resolver's per-prediction `findAlias` N+1** (`canonical_resolver_service.dart:189`) — not worth doing: legacy path is not the production scan path and is being displaced by the ensemble.
- **Renormalising existing device alias rows** (Plan 003's mismatch) — rejected as a data migration: app is pre-release; existing installs are dev devices.
- **First-character fuzzy prefilter defeating OCR errors on char 1** (`getAliasFuzzyCandidates`) — documented as intentional; the classifier/embedder voters cover that failure class. Revisit only with eval evidence.
- **Review-dump data files (`corrections_alias_candidates*.jsonl`, `proposed_new_items.jsonl`) as dead ends** — by design: human-curation inputs (`mine_corrections_aliases.py` says "do NOT auto-merge"). Documented in Plan 016 instead.

## Audit scope note

Not audited (out of the intelligence-layer scope): the OCR capture stack
(`capture_*`, `document_rectifier_*`, opencv build path), scanner camera UX,
the Go backend outside the grocery surface, `landing/`, `store/`.
